#include "types.h"
#include "user.h"
#include "date.h"

// int global[10000];

int main(int argc, char *argv[])
{
  // int local[100000];
  // for(int i=0;i<100000;i++){
  //   local[i]=0;
  // }

  // int *marr = (int *)malloc(10000 * sizeof(int));
  
  if (pgtPrint())
  {
    printf(2, "Error: Cannot print");
  }

  exit();
}
